# 底値を掴め！ DAILY BOTTOM 仕様書 v0.3

ステータス：実装着手用。**数値はすべて初期値**であり、§10のシミュレーションで調整する前提。根拠のある確定値ではない。

---

## 0. v0.2からの変更点

| 指摘 | 対応 |
|---|---|
| 経過時間・下落幅が兆候になる | 底の出現時刻をシナリオと独立に抽選。開始価格・ボラ・下落幅をランダム化。タイマー非表示。TimeBot / DepthBotで漏れを検査（§10） |
| 無BUY時が未定義 | LIVE開始24秒でタイムアウト → NO TRADE（スコア0） |
| 乖離率がボラ依存 | 判定は「底値周辺ATRで正規化した誤差 z」で行う。表示は乖離率% |
| Bot設計 | 単純ルールBot群・時間/下落幅Bot・複合Bot・Oracleを比較。学習用/検証用シードを分離 |
| 熟練者の大当たり日常化 | 複合BotのULTRA以上を約10%に設定 |
| ランクの非単調 | GOD判定窓を廃止し、z閾値に一本化（同一相場内では乖離率と単調） |
| DAILYの標本1 | DAILYは3相場制、合計スコアで比較 |
| ランキング工数 | MVPはローカル記録のみ。サーバーランキングはPhase 2 |
| 見せる情報 | 価格・ローソク足・出来高のみ。形成中の足をティック単位で描画し、足の途中でBUY可能 |

---

## 1. コア定義

- **コアスキル**：値動きに現れる不完全な反転兆候を読み、ダマシとV字反転のリスクを天秤にかけて、底値圏へのBUYを狙う。
- **1相場**：暴落するチャートを見ながら一度だけBUY。真の底値との近さで評価。
- **操作**：BUYボタン1個のみ。
- **表記**：タイトルまたは設定画面に「実在の銘柄・相場とは無関係です。投資助言ではありません。」

---

## 2. 技術前提（仮置き・変更可）

- Webブラウザ（スマホ縦持ち）、TypeScript + Canvas 2D、Vite、静的ホスティング
- ネイティブアプリ化はMVP後に判断
- `src/core/`（生成・判定）はDOM非依存にし、ブラウザ・Nodeシミュレーション・Phase 2のサーバー検証で同一コードを使う

---

## 3. 時間構造

| 項目 | 値 |
|---|---|
| ティック | 100ms |
| 1足 | 8ティック（0.8秒） |
| 事前表示 | 過去12本（開始時点で描画済み） |
| READY | 1.5秒（チャート表示、BUY無効） |
| LIVE | 最大30本（24.0秒）。この間BUY可能 |
| 生成全長 | LIVE 36本（31〜36本目はBUY後の答え合わせ専用） |
| 真の底の足（アンカー） | LIVE 9〜26本目から一様抽選 |
| タイムアウト | LIVE 30本目終了時にBUYなし → NO TRADE |

- 底は最遅でも26本目なので、待ち続けたプレイヤーも最低4本の反発を見てから判断できる。
- ゲーム内のティック番号は `floor((now − liveStart) / 100ms)` で進める。フレーム落ちしても時間基準で進行する。

---

## 4. 画面（LIVE中）

```
┌───────────────────┐
│ DAILY #042    1/3 │ ヘッダ（相場タイプは表示しない）
│ ¥6,482            │ 現在値（大）
│                   │
│   ローソク足       │ 約55%
│   ─ 現在値ライン   │
│                   │
│   出来高バー       │ 約12%
│                   │
│ [      BUY      ] │ 画面下部 全幅 約22%
└───────────────────┘
```

**表示するもの**：ローソク足（直近30本分の幅）、現在値ライン、右軸の価格目盛り3〜4本、出来高バー（数値なし）

**表示しないもの**：時刻軸、経過時間・残り時間、相場タイプ、開始からの下落率%

**描画ルール**
- 形成中の足をティックごとに更新する（ヒゲが伸びて戻る過程が見える）
- Y軸は表示範囲の高値・安値＋余白10%に追従。スケール変更は0.2秒で補間

**BUY**
- 押下時点で「最後に描画したティック」の価格で約定（描画と判定の不一致を防ぐ）
- 多重タップ無効。`navigator.vibrate` 対応端末では振動
- LIVE開始直後からBUY可能

**バックグラウンド遷移（visibilitychange）**
- DAILY：その相場はNO TRADE（ルールとして事前に明示）
- PRACTICE：一時停止し、復帰時にREADY 1.5秒から再開

---

## 5. チャート生成

### 5.1 処理の流れ

```
seed → RNG → チャート単位パラメータ抽選 → シナリオ抽選 → アンカー b 抽選
→ セグメント列構築 → ダマシ配置 → ティック生成 → 受理判定
→（不合格なら同じRNGを進めて再生成、最大200回）→ OHLC・出来高 → Chart
```

200回を超えて不合格が続いた場合は、シナリオを SAUCER に差し替えて再試行する。

### 5.2 決定性（DAILYで全端末が同一チャートを見るために必須）

- PRNG：mulberry32（`Math.imul` による整数演算）
- 正規乱数：Irwin–Hall法（一様乱数12個の和 − 6）
- **生成処理では `Math.log / exp / sin / cos / pow` を使わない**。JSエンジン間で最下位ビットの一致が保証されないため。`Math.sqrt` と四則演算、`Math.round` は可
- 価格は `p = p × (1 + r)` で更新し、出力時に `Math.round` で1円単位に量子化（ゲーム上の簡略化。実際の呼値単位は使わない）
- シード：DAILYは `cyrb53("DAILY|YYYY-MM-DD|i")`（日付はJST固定、i = 0,1,2）。PRACTICEはランダムで、シードを結果画面に表示して再現可能にする
- テスト：固定シード10本の生成結果のハッシュをスナップショット化し、iOS Safari・Android Chrome・Nodeで一致を確認

### 5.3 チャート単位パラメータ

| 項目 | 範囲 |
|---|---|
| 履歴開始価格 | 3,000〜9,000円 一様 |
| V（1足あたり基準ボラ） | 0.7〜1.3% 一様 |
| シナリオ | §5.5の重みで抽選 |
| アンカー b | 9〜26 一様（シナリオと独立に抽選） |

### 5.4 ティック過程

各ティックの収益率：

```
r = m·V/8 + s·(V/√8)·N(0,1) + e
p ← p × (1 + r)
```

- `m`：セグメントのドリフト（V単位／足）
- `s`：ボラ倍率
- `e`：イベント（ヒゲ等）による加算項

足の出来高：

```
vol = 1000 × u × (1 + 0.8·|足の騰落率| / V) × q
q = clamp(1 + 0.25·N(0,1), 0.5, 1.8)
```

- `u`：セグメントの出来高倍率。イベントによる倍率はこの後に乗算する
- 形成中の足の出来高は、ティック進行に比例して積み上げて表示する

### 5.5 シナリオ定義

**共通部（全シナリオ）**

| セグメント | 本数 | m | s | u | 備考 |
|---|---|---|---|---|---|
| 履歴 | 12 | +0.05 | 0.8 | 1.0 | 事前表示分 |
| 初動 | 2〜4 | −0.8 | 1.0 | 1.3 | |
| 下落 | 可変（最低1） | −1.4 | 1.2 | 1.6 | ダマシ配置対象 |

「下落」の本数は、アンカー b に底が来るように逆算する：`b − 初動 − シナリオ固有の底前セグメント合計`。1本未満になる場合は、シナリオ固有セグメントを各下限−1（最低1本）まで縮める。

**A：急落→V字（ID: `V`、重み20%）**

| セグメント | 本数 | m | s | u | イベント |
|---|---|---|---|---|---|
| 加速 | 2〜3 | −2.6 | 1.5 | 2.5 | ダマシ配置対象 |
| 底足（アンカー） | 1 | −1.0 | 1.2 | 2.0 | 真の下ヒゲ p0.8、出来高×3〜5 p0.8 |
| 急反発 | 3〜4 | +2.2 | 1.3 | 2.0 | 1本目 大陽線 p0.7 |
| 戻り | 残り | +0.6 | 1.0 | 1.2 | |

**B：ジリ下げ→底固め（ID: `SAUCER`、重み20%）**

| セグメント | 本数 | m | s | u | 備考 |
|---|---|---|---|---|---|
| 減速 | 4〜6 | −1.0→−0.1（線形） | 1.0→0.6 | 1.2→0.6 | 真の兆候：減速・実体縮小・出来高枯れ |
| 底固め | 5〜8 | 0 | 0.5 | 0.6 | アンカーはこの区間の中央 |
| 上放れ | 残り | +0.9 | 0.8 | 1.4 | |

スパイク系の兆候（下ヒゲ・出来高急増）は出ない。「出来高急増＝底」だけを覚えたプレイヤーを外すためのシナリオ。

**C：ダマシ反発→二段下げ（ID: `FAKE`、重み20%）**

| セグメント | 本数 | m | s | u | イベント |
|---|---|---|---|---|---|
| 第一安値 | 1 | −1.0 | 1.2 | 1.6 | ダマシ下ヒゲ p0.5、ダマシ出来高×3 p0.5 |
| ダマシ反発 | 3〜5 | +1.3 | 1.1 | 1.3 | 1本目 大陽線（ダマシ）p0.5 |
| 二段下げ | 3〜5 | −2.0 | 1.4 | 2.0 | |
| 底足（アンカー） | 1 | −1.0 | 1.2 | 2.0 | 真の下ヒゲ p0.6、出来高×3〜5 p0.7 |
| 反発 | 3〜5 | +1.5 | 1.2 | 1.6 | |
| 残り | 残り | +0.5 | 1.0 | 1.1 | |

受理条件：真の底 ≤ 第一安値 − 2V×価格

**D：二番底（ID: `DOUBLE`、重み15%）**

| セグメント | 本数 | m | s | u | イベント |
|---|---|---|---|---|---|
| 第一底 | 1 | −1.0 | 1.2 | 2.0 | 下ヒゲ p0.5、出来高×3〜4 p0.7 |
| 反発 | 3〜4 | +1.2 | 1.0 | 1.2 | |
| 二番底 | 3〜4 | −1.1 | 1.0 | 0.9 | 出来高は第一底より少ない |
| 上昇 | 残り | +1.4 | 1.1 | 1.4 | |

分岐：
- D1（70%）：二番底が第一底をわずかに下回る。真の底＝二番底（アンカー）。受理条件：第一底 − 1.0V×価格 ≤ 二番底 ≤ 第一底 − 0.05V×価格
- D2（30%）：安値切り上げ。真の底＝第一底（アンカー）。受理条件：第一底 + 0.3V×価格 ≤ 二番底安値 ≤ 第一底 + 1.5V×価格

どちらの分岐かによって、第一底のイベントの `genuine` が変わる。

**E：セリクラ→底固め（ID: `CLIMAX`、重み15%）**

| セグメント | 本数 | m | s | u | イベント |
|---|---|---|---|---|---|
| 加速 | 2〜3 | −2.6 | 1.5 | 2.5 | ダマシ配置対象 |
| クライマックス（アンカー） | 1 | −1.0 | 1.3 | 2.5 | 真の下ヒゲ p0.9（深さ1.5〜2.5V）、出来高×4〜6 p0.9 |
| 底固め | 4〜7 | 0 | 0.6 | 0.8 | クライマックス安値を割らない（受理条件） |
| 上放れ | 残り | +0.8 | 0.9 | 1.3 | |

**F：レンジ→突然反転（ID: `RANGE`、重み10%）**

| セグメント | 本数 | m | s | u | イベント |
|---|---|---|---|---|---|
| 下値レンジ | 6〜9 | −0.2 | 0.7 | 0.8 | アンカーはレンジ後半1/3の中央 |
| 急反転 | 2 | +2.5 | 1.2 | 2.5 | 大陽線 p0.8 |
| 残り | 残り | +0.6 | 1.0 | 1.2 | |

兆候が乏しく、反転確認を待つとジャンプに置いていかれる高難度シナリオ。

### 5.6 イベントの実装

| 種別 | 実装 |
|---|---|
| 下ヒゲ（深さ w、通常 1.2〜2.0V） | 対象足の8ティックのうち t=1〜3 に各 `−w·V/3`、t=4〜6 に各 `+w·V/3 × k`（k: 0.7〜1.0）を e に加算 |
| 出来高スパイク | 対象足の出来高に倍率を乗算 |
| 大陽線 | 対象足の m を +3.0 に上書き |
| 一時減速（ダマシ） | 2〜3本の m を −0.2 に上書き、s を ×0.7 |
| 小反発（ダマシ） | 1〜2本の m を +1.0 に上書き |

生成したイベントはすべて `SignalEvent`（§11）として記録する。

### 5.7 ダマシの配置

- 配置先：「下落」「加速」セグメント内。真の底の足の3本以上前
- 個数 k：0 / 1 / 2 / 3 をそれぞれ 20% / 40% / 30% / 10% で抽選
- 種別：下ヒゲ 30% / 出来高スパイク 30% / 一時減速 20% / 小反発 20%
- 20%の確率で2種を同じ足に重ねる

**狙い**：ダマシの大半は単独の兆候、真の底は複数の兆候が重なりやすい。これにより「複合的に読める人ほど期待値が上がる」構造にする。

### 5.8 受理条件

1. 全体最安値（量子化後のティック価格）の足が、アンカー±2本以内かつLIVE 9〜26本目にある
2. LIVE最終足の終値 ≥ 最安値 + 3 × ATR_ref（明確な反発がある）
3. LIVE開始価格から最安値までの下落率が12〜45%
4. シナリオ固有の条件（C、D、E）を満たす

最安値と同値のティックが複数ある場合は、最初のティックを底とする（これは条件ではなく定義）。

---

## 6. 判定

| 記号 | 定義 |
|---|---|
| B | 底値 = LIVE全36本での最安値ティック価格（BUY後に形成された場合も含む） |
| d | 乖離率 = (BUY価格 − B) / B（常に0以上。小数2桁の%で表示） |
| ATR_ref | 底の足の前後5本（計11本）の真の値幅（True Range）の平均 |
| z | 正規化誤差 = (BUY価格 − B) / ATR_ref |

**ランク（初期値。§10で調整）**

| ランク | 条件 |
|---|---|
| GOD BOTTOM | z ≤ 0.2 |
| ULTRA BOTTOM | z ≤ 0.5 |
| SUPER BOTTOM | z ≤ 1.0 |
| GREAT | z ≤ 2.0 |
| GOOD | z ≤ 4.0 |
| BAD | z > 4.0 |
| NO TRADE | タイムアウト、またはDAILY中のバックグラウンド遷移 |

- 同一相場内では、ランクは乖離率に対して単調。
- 相場が違うと、同じ乖離率でもランクが変わりうる（ATRで補正しているため）。結果画面に「この相場の値幅を基準に判定」と明記する。
- 精度スコア = `round(1000 / (1 + z))`。NO TRADEは0。
- DAILY合計 = 3相場のスコアの和（最大3000）。

---

## 7. モード

### 7.1 DAILY

- 1日3相場。JST 0:00に更新
- 3相場のシナリオは重み付き抽選で、重複しないように選ぶ
- 各相場は1回のみ。相場を開始した時点で `started` をローカル保存し、リロードしてもやり直せないようにする（未完了のまま再訪した場合はNO TRADE）
- 記録はlocalStorage（端末ローカル）に保存
- 番号：リリース日を #001 とする
- シナリオ名と兆候マーカーは、3相場すべて終了した後にのみ表示する（ネタバレ防止）

### 7.2 PRACTICE

- ランダムシード、回数無制限
- 結果画面にシナリオ名と兆候マーカーを表示する（学習用）
  - 真の兆候：色付きラベル（例「本物の下ヒゲ」）
  - ダマシ：グレーのラベル（例「ダマシの出来高急増」）
- 端末内に統計を保存：プレイ回数、ランク分布、平均スコア、シナリオ別平均スコア

---

## 8. 演出

### 8.1 状態遷移

```
TITLE → READY → LIVE →(BUY)→ FREEZE → PLAYBACK → REVEAL → RESULT
               └→(タイムアウト)→ PLAYBACK → RESULT(NO TRADE)
```

- **FREEZE**（0.4秒）：画面を停止し、効果音を鳴らす枠
- **PLAYBACK**：BUY以降の生成済みデータをLIVE 36本目まで再生する
  - 通常0.2秒/足。底の足とその前後1本だけ0.5秒/足
  - 合計の上限は4秒。超える場合は通常速度の部分を圧縮する
  - **表示するのは生成済みの実データそのもの。演出のために値を変えない**
- **REVEAL**：ランク別の演出（下表）。開始0.5秒後からタップでスキップ可能

| ランク | 時間 | 内容（MVPではテキストと図形のプレースホルダー） |
|---|---|---|
| BAD | 2秒 | テキストのみ |
| GOOD | 3秒 | テキスト |
| GREAT | 4秒 | 小フラッシュ |
| SUPER | 5秒 | フラッシュ＋カットイン枠 |
| ULTRA | 7秒 | 暗転 → カットイン枠 → 全画面をローソク足が突き抜ける |
| GOD | 10秒 | 無音2秒 → 黒画面「約定しました。」→ 金色ローソク → 4人カットイン枠 → 専用BGM枠 |

---

## 9. 結果画面・シェア

### 9.1 結果画面の表示項目

- BUY価格、底値、乖離率、ランク、精度スコア
- チャート全体（YOUマーカーとBOTTOMマーカー付き）
- PRACTICE：シナリオ名、兆候マーカー、シード
- ボタン：シェア / 次の相場（DAILY）/ もう一度（PRACTICE）

### 9.2 シェアテキスト（DAILY）

ネタバレ防止のため、価格は含めない。

```
底値を掴め！ DAILY BOTTOM #042
🟪🟦🟥  2,184 / 3,000
最高：ULTRA BOTTOM
#底値を掴め #株クラRPG
https://（URL）
```

ランクと絵文字の対応：GOD 👑 / ULTRA 🟪 / SUPER 🟦 / GREAT 🟩 / GOOD 🟨 / BAD 🟥 / NO TRADE ⬛

共有方法：Web Share API に対応した端末ではネイティブの共有シートを使う。非対応の場合はXの投稿intent URLを開く（URL形式は実装時に現行仕様を確認すること）。

---

## 10. シミュレーション（Codexで実装）

`src/core` をNodeから直接呼び出して実行する。GUIは不要。

### 10.1 Bot一覧

Botは観測可能な情報（そのティックまでに描画された内容）のみを使う。RandomBot以外の全Botには反応遅延 N(250ms, 50ms) を加え、ティック単位に丸める。

| Bot | 行動 | 目的 |
|---|---|---|
| RandomBot | LIVE中の一様ランダムなティックでBUY | 運だけの基準線 |
| TimeBot(t) | 常にLIVE開始t秒でBUY。t=3〜23を0.5秒刻みで走査し、最良のtを採用 | 時間による情報漏れの検査 |
| DepthBot(x) | 開始価格からの下落率がx%に達したらBUY。x=10〜45を1%刻みで走査し、最良のxを採用 | 下落幅による情報漏れの検査 |
| WickBot | 下ヒゲ ≥ 1.0V の足が確定した時点でBUY | 単純ルール |
| VolumeBot | 出来高が直近10本平均の2.2倍以上になった足でBUY | 単純ルール |
| ConfirmBot(n) | 安値更新の後、陽線がn本連続したらBUY（n=1,2,3） | 単純ルール（反転確認型） |
| ComboBot | 兆候スコア（下ヒゲ・出来高・減速・安値を更新していない経過本数・大陽線の重み付き和）が閾値を超えたらBUY。重みと閾値は学習用シードでグリッドサーチ | 熟練プレイヤーの代理 |
| OracleBot | 真の底のティック＋反応遅延でBUY | GODに到達可能かの上限確認 |

- 学習用シード10,000本と検証用シード10,000本を分ける。報告する数値はすべて検証用シードで算出する
- 出力：
  - Bot別のランク分布
  - Bot × シナリオ別の平均スコア
  - 兆候ごとの実測信頼度（兆候が出現した足の±2本以内に真の底がある割合）

### 10.2 合格基準（初期目標）

| # | 項目 | 基準 |
|---|---|---|
| 1 | スキルの存在 | ComboBot：SUPER以上 25〜35%、ULTRA以上 8〜12%、GOD 1〜3% |
| 2 | 運の上限 | RandomBot：SUPER以上 ≤ 5% |
| 3 | 情報漏れがない | TimeBot・DepthBotの最良値：SUPER以上 ≤ RandomBot + 3pt |
| 4 | 支配戦略がない | 単純ルールBotの最良値：SUPER以上 ≤ 15%。かつ、6シナリオ中5つ以上で同じ単純Botが最良にならない |
| 5 | GODに到達可能 | OracleBot：GOD ≥ 30% |
| 6 | 兆候の信頼度 | 実測値が設計値の±10pt以内（下ヒゲ70%、大陽線65%、出来高急増55%、減速45%、小反発20%） |

**調整の順序**
1. まず情報漏れ（#3）を潰す
2. 次に支配戦略（#4）を潰す
3. 最後にランク閾値を調整する（#1, #2, #5）

閾値をいじってもComboBotと単純Botの差が出ない場合は、閾値ではなく生成側（ダマシの出現率、兆候の重なり方）を直すこと。

**注意**：Botが合格しても、人間にとって読みやすいかは検証できていない。Phase 1の終わりに5〜10人のプレイテストが必須。

---

## 11. データ構造とモジュール構成

```ts
type ScenarioId = 'V' | 'SAUCER' | 'FAKE' | 'DOUBLE' | 'CLIMAX' | 'RANGE';
type Rank = 'GOD' | 'ULTRA' | 'SUPER' | 'GREAT' | 'GOOD' | 'BAD' | 'NO_TRADE';

interface Candle { o: number; h: number; l: number; c: number; v: number; }

interface SignalEvent {
  candleIndex: number;   // LIVE基準
  kind: 'WICK' | 'VOLUME' | 'DECEL' | 'BIG_GREEN' | 'SMALL_BOUNCE';
  genuine: boolean;      // 真の兆候 or ダマシ
}

interface Chart {
  seed: number;
  generatorVersion: string;
  scenario: ScenarioId;
  history: Candle[];     // 12本
  ticks: number[];       // LIVE全ティックの価格（整数円）。36本 × 8ティック = 288
  candles: Candle[];     // LIVE全36本
  bottomTick: number;
  atrRef: number;
  events: SignalEvent[];
}

interface Result {
  buyTick: number | null;
  buyPrice: number | null;
  bottomPrice: number;
  deviationPct: number | null;
  z: number | null;
  rank: Rank;
  score: number;
}

// 純粋関数
declare function generateChart(seed: number, opts?: { scenario?: ScenarioId }): Chart;
declare function judge(chart: Chart, buyTick: number | null): Result;
```

```
src/core/rng.ts          mulberry32, cyrb53, normal（Irwin–Hall）
src/core/scenarios.ts    シナリオ定義（データのみ）
src/core/generator.ts    generateChart
src/core/judge.ts        judge
src/game/state.ts        状態遷移（§8.1）
src/render/              Canvas描画（チャート・出来高・演出）
src/modes/daily.ts
src/modes/practice.ts
sim/bots.ts
sim/run.ts               Nodeで実行。レポートはCSVとMarkdownで出力
tests/determinism.test.ts
tests/judge.test.ts
```

---

## 12. スコープ

**Phase 1（MVP）**
- core一式、シミュレーション、PRACTICE、DAILY（ローカル記録）、シェア
- 演出はプレースホルダー
- テスト（決定性・判定）

**Phase 2**
- キャラクターのカットイン、BGM・SE
- サーバーランキング

**Phase 3候補**
- シナリオの追加、難易度別モード

**サーバーランキングの方針（Phase 2）**
- クライアントは日付とBUYティック番号3つだけを送信し、サーバーが同じcoreで再計算してスコアを確定する
- これでスコアの改ざんは防げる
- ただし、チャートの事前把握（先にプレイした人からの情報、クライアントの解析）は防げない。1日3相場・各1回という設計で、影響を割り切る

---

## 13. 未検証事項・リスク

- 0.8秒/足で人間が兆候を読めるか不明。プレイテストで0.6 / 0.8 / 1.2秒を比較する
- ComboBotの成績は人間の熟練者の代理にすぎない
- シナリオ6種は、数十回のプレイで見分けられるようになる可能性がある。飽きへの対策は検証の後で判断する
- ドリフト・確率・閾値などの数値はすべて初期値であり、根拠のある確定値ではない
- タップの入力遅延には端末差がある。MVPでは補正しない
